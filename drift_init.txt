Add this code snippet after the startBalanceRefresh() call in connectW():

    // Initialise Drift client and refresh live market data
    setTimeout(async () => {
      await initDriftClient().catch(console.error);
      await refreshCoins().catch(console.error);
    }, 500);
